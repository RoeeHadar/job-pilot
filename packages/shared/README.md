# Shared contracts

Generate TypeScript types from the FastAPI OpenAPI schema once the API stabilizes:

```bash
# from apps/api with server running
curl http://127.0.0.1:8000/openapi.json -o openapi.json
```
